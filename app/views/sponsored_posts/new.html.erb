<h1>New Sponsored Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Sponsored Posts:</p>
    <ul>
      <li>Make sure the Sponsored Post is appropriate.</li>
      <li>Make sure the price is firm.</li>
      <li>Make sure the ad is funny.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @sponsored_posts do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter sponsored ad title." %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsored ad details." %>
      </div>
      
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>
